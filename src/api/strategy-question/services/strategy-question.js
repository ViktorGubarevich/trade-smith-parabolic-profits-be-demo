'use strict';

/**
 * strategy-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::strategy-question.strategy-question');
